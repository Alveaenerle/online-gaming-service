services:
  backend:
    build:
      context: ./backend
      target: production
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080

  frontend:
    build:
      context: ./frontend
      target: production
    ports:
      - "80:80"      
    depends_on:
      - backend